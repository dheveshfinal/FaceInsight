# ==============================================================
#  FaceInsight – Render Deployment Configuration (Fixed)
#  For deploying backend to Render.com
# ==============================================================

services:
  # ── PostgreSQL Database ────────────────────────────────────
  - type: pserv
    name: faceinsight-postgres
    image: postgres:16
    region: oregon
    plan: standard
    envVars:
      - key: POSTGRES_USER
        value: faceinsight
      - key: POSTGRES_PASSWORD
        sync: false
      - key: POSTGRES_DB
        value: faceinsight_db
      - key: POSTGRES_INITDB_ARGS
        value: "-c max_connections=200"

  # ── Qdrant Vector Database ────────────────────────────────
  - type: pserv
    name: faceinsight-qdrant
    image: qdrant/qdrant:latest
    region: oregon
    plan: standard
    disks:
      - name: qdrant-storage
        mountPath: /qdrant/storage
        sizeGB: 10

  # ── FastAPI Backend (with embedded Celery) ───────────────────
  - type: web
    name: faceinsight-api
    runtime: docker
    region: oregon
    plan: standard
    repo: https://github.com/dheveshfinal/faceinsight
    dockerfilePath: backend/Dockerfile
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && bash start.sh
    
    envVars:
      - key: PORT
        value: 8000
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_SECRET_KEY
        sync: false
      - key: JWT_SECRET_KEY
        sync: false
      - key: DATABASE_URL
        fromService:
          name: faceinsight-postgres
          property: databaseURL
      - key: REDIS_URL
        sync: false
      - key: CELERY_BROKER
        sync: false
      - key: CELERY_BACKEND
        sync: false
      - key: QDRANT_URL
        value: http://faceinsight-qdrant:6333
      - key: GROQ_API_KEY
        sync: false
      - key: CORS_ORIGINS
        value: http://localhost:3000,https://your-vercel-app.vercel.app
      - key: ALLOWED_HOSTS
        value: faceinsight-api.onrender.com,localhost
      - key: ML_DEVICE
        value: cpu
      - key: MAX_UPLOAD_SIZE_MB
        value: 10
    
    healthCheckPath: /api/v1/health
