{
  "version": 2,
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable $HOME/flutter && export PATH=$PATH:$HOME/flutter/bin && flutter precache && cd flutter_app && flutter pub get && flutter build web --web-renderer html --dart-define API_BASE_URL=https://faceinsight-y0sc.onrender.com/api/v1 --dart-define WS_BASE_URL=wss://faceinsight-y0sc.onrender.com/ws --release",
  "outputDirectory": "flutter_app/build/web",
  "framework": null,
  "routes": [
    {
      "src": "/static/(.*)",
      "headers": {
        "cache-control": "public, max-age=31536000, immutable"
      },
      "destination": "/static/$1"
    },
    {
      "src": "/.*",
      "destination": "/index.html"
    }
  ]
}